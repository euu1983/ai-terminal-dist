# AI Terminal Distribution

This repository hosts the binary distribution for AI Terminal daemon.

**Install:**

```bash
# macOS / Linux
curl -fsSL https://dist.ai-terminal.org/install.sh | bash

# Windows (PowerShell)
iwr -useb https://dist.ai-terminal.org/install.ps1 | iex
```
